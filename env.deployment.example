# 部署环境变量配置示例
# 复制此文件为 .env 并根据需要修改

# ============================================
# 数据库配置
# ============================================
# PostgreSQL数据库密码
POSTGRES_PASSWORD=123456

# ============================================
# Redis配置
# ============================================
# Redis密码（留空表示不需要密码）
REDIS_PASSWORD=

# ============================================
# JWT配置
# ============================================
# JWT密钥（重要：生产环境请使用强随机字符串）
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ============================================
# 域名配置
# ============================================
# Nginx域名（用于SSL证书和配置）
NGINX_DOMAIN=your-domain.com
# SSL域名（用于自动生成自签名证书，默认使用NGINX_DOMAIN）
SSL_DOMAIN=${NGINX_DOMAIN}

# ============================================
# 可选配置
# ============================================
# 如果需要自定义其他配置，可以在这里添加
# 例如：
# GIN_MODE=release
# SERVER_PORT=8080

# ============================================
# 使用说明
# ============================================
# 1. 复制此文件：cp env.deployment.example .env
# 2. 修改敏感信息（密码、密钥等）
# 3. 运行：docker-compose --profile production up -d
#
# 安全提醒：
# - 不要将包含真实密码的.env文件提交到版本控制系统
# - 生产环境使用强密码和随机密钥
# - 定期更换密码和密钥
