# Line账号管理系统 - 前端项目

## 📋 项目概述

基于 Vue3 + Element Plus + Vite 构建的管理系统前端。

## 🚀 快速开始

### 环境要求

- Node.js 18+
- npm 或 yarn

### 安装依赖

```bash
npm install
```

### 配置环境变量

在项目根目录创建 `.env.development` 和 `.env.production` 文件：

**.env.development** (开发环境)
```env
VITE_API_BASE_URL=/api
VITE_WS_URL=ws://localhost:8080/api/ws
```

**.env.production** (生产环境)
```env
VITE_API_BASE_URL=/api
VITE_WS_URL=wss://your-domain.com/api/ws
```

### 运行开发服务器

```bash
npm run dev
```

前端将在 `http://localhost:3000` 启动

### 构建生产版本

```bash
npm run build
```

构建产物将输出到 `dist` 目录

## 📁 项目结构

```
frontend/
├── src/
│   ├── api/              # API封装
│   │   ├── auth.js       # 认证相关API
│   │   ├── group.js      # 分组管理API
│   │   ├── lineAccount.js # Line账号API
│   │   ├── customer.js   # 客户管理API
│   │   ├── followUp.js   # 跟进记录API
│   │   ├── stats.js      # 统计数据API
│   │   ├── contactPool.js # 底库管理API
│   │   ├── user.js       # 用户管理API
│   │   └── llm.js        # 大模型API
│   │
│   ├── assets/           # 静态资源
│   │
│   ├── components/       # 公共组件
│   │
│   ├── router/           # 路由配置
│   │   └── index.js
│   │
│   ├── store/            # Pinia状态管理
│   │   └── auth.js       # 认证状态
│   │
│   ├── styles/           # 样式文件
│   │   ├── variables.less # 样式变量
│   │   ├── mixins.less   # 样式混合
│   │   └── index.less    # 全局样式
│   │
│   ├── utils/            # 工具函数
│   │   ├── request.js    # Axios封装
│   │   ├── websocket.js  # WebSocket封装
│   │   ├── auth.js       # 认证工具
│   │   └── format.js     # 格式化工具
│   │
│   ├── views/            # 页面组件
│   │   ├── auth/         # 认证页面
│   │   │   ├── Login.vue           # 登录页
│   │   │   └── SubAccountLogin.vue # 子账号登录
│   │   ├── admin/        # 管理后台
│   │   │   ├── Layout.vue          # 布局
│   │   │   ├── Dashboard.vue      # 首页
│   │   │   ├── GroupManage.vue    # 分组管理
│   │   │   ├── AccountList.vue    # 账号列表
│   │   │   ├── LeadsList.vue      # 线索列表
│   │   │   ├── ContactPool.vue    # 底库管理
│   │   │   ├── CustomerList.vue   # 客户列表
│   │   │   ├── FollowUpRecords.vue # 跟进记录
│   │   │   ├── UserManage.vue      # 用户管理
│   │   │   ├── LLMConfig.vue      # 大模型配置
│   │   │   └── LLMCallLogs.vue    # 调用记录
│   │   └── subaccount/   # 子账号后台
│   │       └── Layout.vue
│   │
│   ├── App.vue
│   └── main.js
│
├── index.html
├── package.json
├── vite.config.js
└── README.md
```

## 🔧 技术栈

- **Vue 3** - 渐进式JavaScript框架
- **Vue Router 4** - 官方路由管理器
- **Pinia** - 状态管理
- **Element Plus** - UI组件库
- **Axios** - HTTP客户端
- **Vite** - 构建工具
- **Less** - CSS预处理器
- **ECharts** - 图表库
- **Day.js** - 日期处理库

## 📝 功能模块

### 已实现

- ✅ 项目基础结构
- ✅ 路由配置（含路由守卫）
- ✅ 认证状态管理（Pinia）
- ✅ Axios请求封装（含拦截器）
- ✅ WebSocket工具类
- ✅ 登录页面（管理员/普通用户）
- ✅ 子账号登录页面
- ✅ 管理后台布局
- ✅ 子账号后台布局
- ✅ 基础页面组件占位
- ✅ API封装（所有模块）
- ✅ 工具函数（格式化、认证等）
- ✅ 样式系统（Less变量、混合）

### 待实现

- ⏳ 各功能模块的完整实现
- ⏳ WebSocket实时数据更新
- ⏳ 数据可视化（ECharts）
- ⏳ 响应式布局优化
- ⏳ 错误处理与提示优化

## 🔐 认证流程

1. 用户访问需要认证的页面
2. 路由守卫检查token
3. 如果未登录，重定向到登录页
4. 登录成功后，保存token和用户信息
5. 后续请求自动携带token

## 🌐 API代理配置

开发环境下的API代理配置在 `vite.config.js` 中：

```javascript
proxy: {
  '/api': {
    target: 'http://localhost:8080',
    changeOrigin: true,
  },
  '/ws': {
    target: 'ws://localhost:8080',
    changeOrigin: true,
    ws: true,
  }
}
```

## 📦 构建与部署

### 开发模式

```bash
npm run dev
```

### 生产构建

```bash
npm run build
```

### 预览构建结果

```bash
npm run preview
```

## 🐛 问题排查

### 无法连接后端API

1. 检查后端服务是否运行在 `http://localhost:8080`
2. 检查 `vite.config.js` 中的代理配置
3. 检查环境变量配置

### 登录后无法跳转

1. 检查路由配置是否正确
2. 检查token是否正确保存
3. 检查路由守卫逻辑

### Element Plus图标不显示

确保已正确导入图标组件：
```javascript
import { User, Lock } from '@element-plus/icons-vue'
```

## 📄 许可证

MIT License

